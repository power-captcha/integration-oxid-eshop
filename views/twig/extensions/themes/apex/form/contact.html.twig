{% extends "form/contact.html.twig" %}
{% block captcha_form %}
    <div class="mb-3">
        {% include '@power_captcha/templates/widget.html.twig' with {pcUserInputField: '#loginUser'} %}

        {% if Errors.powerCaptchaErrors %}
        <div class="mt-1 text-danger">
            <p>{{ translate({ ident: "POWER_CAPTCHA_CONFIRM_SECURITY_CHECK_HINT" }) }}</p>
        </div>
        {% endif %}
    </div>
{% endblock %}
